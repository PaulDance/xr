# Runs all the available tests.
function _run_tests() {
    # Implementation here.
}

# Copies the stored benchmarks and runs them.
function _run_benches() {
    # Leave this and fill _ with the track's name or add an implementation.
    echo "The _ track does not support benchmarks." >&2
    return 1
}
